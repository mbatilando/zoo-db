extends ../layout

block content
	table(class="pure-table")
		thead
			tr
				th id
				th name
				th time
				th ExhibitId
		tbody
			each show, index in Shows
				tr(class=(index % 2 == 0) ? 'pure-table-even' : 'pure-table-odd')
					td #{show.id}
					td #{show.name}
					td #{show.time}
					td #{show.ExhibitId}
