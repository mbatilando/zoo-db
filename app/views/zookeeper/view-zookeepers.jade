extends ../layout

block content
	table(class="pure-table")
		thead
			tr
				th id
				th first_name
				th last_name
				th phone
				th work_days
				th ZooId
		tbody
			each zooKeeper, index in Zookeepers
				tr(class=(index % 2 == 0) ? 'pure-table-even' : 'pure-table-odd')
					td #{zooKeeper.id}
					td #{zooKeeper.first_name}
					td #{zooKeeper.last_name}
					td #{zooKeeper.phone}
					td #{zooKeeper.work_days}
					td #{zooKeeper.ZooId}